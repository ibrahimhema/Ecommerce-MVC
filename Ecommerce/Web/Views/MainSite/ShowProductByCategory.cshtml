@model  List<BL.ViewModels.ProductViewModel>
@{
    ViewBag.Title = "ShowProductByCategory";
    Layout = "~/Views/Shared/_MainSiteLayoutView.cshtml";
    var x = Model;
}
<div class="container">
    <div style="margin-top:100px"></div>
    @{
        Html.RenderPartial("_SlickPrtialView", Model);

    }
</div>


@*<div class="container">
    <div class="row">
        @foreach (var product in Model)
        {
        <div class="col-md-4 col-sm-6">
            @{
                Html.RenderAction("ProductPatialView", product);
            }
        </div>
                }
        </div>
</div>*@
     
        
        
        
